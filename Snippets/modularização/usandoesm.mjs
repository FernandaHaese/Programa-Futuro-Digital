import { adicionar, subtrair } from './moduloesm.mjs';

const soma = adicionar(5, 3);
const diferenca = subtrair(5, 3);

console.log("Soma de 5 + 3 =", soma);
console.log("Subtração de 5 - 3 =", diferenca);